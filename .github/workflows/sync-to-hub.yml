name: Sync to Hugging Face Hub
on:
  push:
    branches: [main]
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true
      
      - name: Sync to Hugging Face Spaces
        uses: huggingface/hub-sync@v0.1.0
        with:
          github_repo_id: thehmfpk/safex-ai-chatbot
          huggingface_repo_id: thehmfpk/safex-ai-chatbot
          hf_token: ${{ secrets.HF_TOKEN }}
          repo_type: space
          space_sdk: docker
